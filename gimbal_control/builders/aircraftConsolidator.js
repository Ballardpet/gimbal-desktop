/**
 * This will be the file to consolidate aircraft JSON files
 * 
 * For now we'll read from the ADSB and P5 files, but eventually we'll add other tracking types as required
 * 
 * Let's track: Latitude, Longitude, Altitude, id (hexid for adsb, callsign for p5, whatever for other tracking types), tracking type
 * 
 * Adding tracking type is important so we can have a filter on the map eventually
 * 
 * Reasearch this once ready for the map: https://leafletjs.com/
 */